# 🧹 MaintenanceDeluxe v0.1.1 — Rebuild + retrait icône trompeuse + manifest cleanup

## 🔄 Modifié

- 🧹 DLL rebuild propre depuis source clean (la précédente avait été built dans un environnement de test).
- 🖼️ Retrait de l'**icône plugin** (logo film reel) qui était présente dans le bundle mais que Jellyfin n'utilisait pas — diminution taille bundle.
- 📋 Manifest : suppression du doublon de version (`versions[0]` et `versions[1]` pointaient sur le même tag par erreur).
