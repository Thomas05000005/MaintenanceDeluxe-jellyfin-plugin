# 🚨 MaintenanceDeluxe v0.1.5 — Hotfix critique : v0.1.4 était broken

## 🚨 Hotfix critique

- 🐛 v0.1.4 contenait une **syntax error** dans `MD_CSS` (virgule manquante avant le block `@supports metal-border`). Le script inline JavaScript échouait à parser, tout le plugin se cassait silencieusement.
- ✅ **Fix** : virgule ajoutée, plus check `node --check` plus strict côté CI pour catch ce genre de bug avant release.
